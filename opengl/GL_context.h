#ifndef __GL_CONTEXT_H
#define __GL_CONTEXT_H

#include "GL_defines.h"

// we have to make sure gl objects should be acccessible across threads

typedef struct {

} gl_context;

#endif // __GL_CONTEXT_H
